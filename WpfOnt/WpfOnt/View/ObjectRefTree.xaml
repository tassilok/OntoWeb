<UserControl x:Class="WpfOnt.View.ObjectRefTree"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:viewModel="clr-namespace:WpfOnt.ViewModel"
             mc:Ignorable="d" Height="350.215" Width="388.841">
    <UserControl.DataContext>
        <viewModel:ObjectRefTreeModel/>
    </UserControl.DataContext>
    <Grid>
        <Grid.Resources>
            <HierarchicalDataTemplate ItemsSource="{Binding ChildNodes}" x:Key="TreeTemplate">
                <StackPanel Orientation="Horizontal" >
                    <TextBlock 
                                Visibility="Collapsed" 
                                Text="{Binding IdNode}" />
                    <TextBlock 
                                Text="{Binding NameNode}"/>
                </StackPanel>

            </HierarchicalDataTemplate>
        </Grid.Resources>
        <TreeView ItemsSource="{Binding NodeList}" ItemTemplate="{StaticResource TreeTemplate}">
           
        </TreeView>
        
    </Grid>
</UserControl>
